import { jsx as _jsx, jsxs as _jsxs } from "react/jsx-runtime";
import { DescriptionListDescription, DescriptionListGroup, DescriptionListTerm, } from '@patternfly/react-core';
/**
 * A wrapper around PatternFly's description group. This component's parent must
 * be a PatternFly DescriptionList.
 */
export const OverviewDetailItem = ({ title, isLoading = false, children, error, valueClassName, }) => {
    let status;
    if (error) {
        status = _jsx("span", { className: "pf-v6-u-text-color-subtle", children: error });
    }
    else if (isLoading) {
        status = _jsx("div", { className: "skeleton-text" });
    }
    else {
        status = children;
    }
    return (_jsxs(DescriptionListGroup, { children: [_jsx(DescriptionListTerm, { "data-test": "detail-item-title", children: title }), _jsx(DescriptionListDescription, { className: valueClassName, "data-test": "detail-item-value", children: status })] }));
};
//# sourceMappingURL=OverviewDetailItem.js.map