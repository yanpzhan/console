import * as React from 'react';
import { ShowdownOptions, ShowdownExtension } from 'showdown';
import './MarkdownView.scss';
export type MarkdownProps = {
    content?: string;
    emptyMsg: string;
    exactHeight?: boolean;
    truncateContent?: boolean;
    extensions?: ShowdownExtension[];
    renderExtension?: (contentDocument: Document, rootSelector: string) => React.ReactNode;
    inline?: boolean;
    options?: ShowdownOptions;
    theme?: string;
    updateThemeClass?: (htmlTagElement: HTMLElement, theme: string) => void;
};
export declare const MarkdownView: React.FC<MarkdownProps>;
